[build]
  publish = "dist"
  command = "npm run build"
  functions = "netlify/functions"

[build.bundling]
  target = "es2020"  # Modern JS for functions

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200